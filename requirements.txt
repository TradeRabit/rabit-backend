# Core dependencies
anthropic>=0.7.0
python-dotenv>=1.0.0
websockets>=11.0
aiohttp>=3.8.0
pydantic>=2.0.0
pydantic-settings>=2.0.0

# FastAPI & Web Server
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.9

# Exchange SDKs
# Solana & Drift read-only account tools use an optional dependency set.
# See requirements-drift-readonly.txt for the verified package group.

# Backpack Exchange SDK
bpx-py>=1.1.4
PyNaCl>=1.5.0
PyJWT>=2.8.0

# Additional utilities
tiktoken>=0.5.0
requests>=2.28.0

# Mem0 - User Memory Management
mem0ai>=0.0.1

# Web Search - DuckDuckGo (100% FREE)
ddgs>=9.0.0
